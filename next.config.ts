import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['fakestoreapi.com','images.pexels.com','www.shutterstock.com'],
  },
};

export default nextConfig;
